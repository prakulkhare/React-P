import React from 'react'; //core library
import ReactDOM from 'react-dom/client';// IMPLEMENATION IN WEB
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

